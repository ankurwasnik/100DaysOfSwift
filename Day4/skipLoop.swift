// Skip entry in loop
let fruits  = ["Apple", "Banana", "Peaches", "Pomegrant"]

for fruit in fruits {
    if fruit == "Peaches" {
        continue        
    }
    print("I am eating = \(fruit)")
    
}